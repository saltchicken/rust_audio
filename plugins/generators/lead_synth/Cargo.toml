[package]
name = "lead_synth_plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
clack-plugin = "0.1"
serde = { version = "1.0", features = ["derive"] }
plugin_core = { path = "../../core" }
