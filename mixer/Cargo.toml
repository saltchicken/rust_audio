[package]
name = "rust_audio_mixer"
version = "0.1.0"
edition = "2021"

[dependencies]
cpal = { version = "0.15", features = ["jack"] }
ringbuf = "0.3"
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
clack-host = "0.1"
