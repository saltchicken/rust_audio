# Cargo.toml
[workspace]
members = [
    "mixer",
    "plugins/core",
    "plugins/effects/*",
    "plugins/generators/*"
]
resolver = "2"

# Optimize all plugins and the mixer together
[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
